<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head.ejs', { title: "Edit " + fruit.name }) %>
  <body>
    <%- include("../partials/navbar.ejs")%>
    <h1>Edit the <%= fruit.name %></h1>
    <form action="/fruits/<%= fruit._id %>?_method=PUT" method="post">
      <label for="name">Name:</label>
      <input type="text" name="name" id="name" value="<%= fruit.name %>" />

      <label for="color">Color:</label>
      <input type="text" name="color" id="color" value="<%= fruit.color %>" />

      <div class="checkbox-div">
        <label for="isReadyToEat">Ready to Eat?</label>
        <input
          type="checkbox"
          name="isReadyToEat"
          id="isReadyToEat"
          <% if(fruit.isReadyToEat){%> checked <% } %> 
          />
      </div>
      <button type="submit">Submit</button>
    </form>
  </body>
</html>
